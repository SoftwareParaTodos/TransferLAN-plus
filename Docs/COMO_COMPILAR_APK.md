# Cómo compilar APK

Subí esta versión al repo y ejecutá:

```text
Actions → Build Android APK → Run workflow
```

Si compila bien, descargá:

```text
TransferLANPlus-v1.0.6-beta-debug-apk
```

Adentro está:

```text
app-debug.apk
```
